const String baseUrl = 'https://breakingbadapi.com/api';

abstract class EndPoints {
  static const String characters = '$baseUrl/characters';
  static const String episodes = '$baseUrl/episodes';
}
